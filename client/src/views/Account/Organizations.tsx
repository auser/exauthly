import * as React from 'react';

import styled from 'styled-components';

import AccountLayout from './AccountLayout';

interface Props {}

export const Organizations: React.SFC<Props> = props => {
  return <div>Organizations goes here</div>;
};

export default AccountLayout(Organizations);
